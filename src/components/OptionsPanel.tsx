/**
 * Options panel
 * Displays conversion options based on selected target format
 * Reserved for future format-specific options
 */

export function OptionsPanel() {
  // No format-specific options currently available
  // This component is reserved for future use
  return null
}
